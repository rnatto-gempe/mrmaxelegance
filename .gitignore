# Ferramenta interna de pesquisa — NÃO publicar
# (contém perfil do Chrome com sessão do WhatsApp Web, chats e ~380 MB de dados)
radar/

# Matéria-prima da geração: dump da taxonomia e dos vídeos do acervo de
# origem. Não sobe — o repositório é público e o acervo é do fornecedor.
# Regerar leva um minuto: python3 tools/raspa-stlflix.py
tools/dados/

# Segredos. O `.env` guarda a chave pessoal do PostHog, que lê e escreve a
# conta inteira — e este repositório é público. Só `.env.exemplo` sobe.
.env
.env.*
!.env.exemplo

# Sistema / editor
.DS_Store
.playwright-mcp/
*.log

# Ambientes
.venv/
node_modules/
